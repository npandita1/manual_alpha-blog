<h1>Showing Article Details</h1>

<p><strong>Title: </strong><%= @article.title%></p>
<p>
  Tags:
  <% @article.tags.each do |tag| %>
    <%= link_to tag.name, tag_path(tag) %>
  <% end %>
</p> 
<p><strong>Description: </strong><%= @article.description%></p>

<%= render 'comments/show_comments' %>

<%= render 'comments/comment_form' %>

<%= link_to 'Edit', edit_article_path(@article) %> | <%= link_to 'Return to articles listing', articles_path %> | 
<%= button_to 'Delete', article_path(@article.id), method: :delete, data: {turbo_confirm: "Are you sure?"} %>